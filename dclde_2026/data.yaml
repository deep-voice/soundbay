# This is a template. The real file will be generated by
# preprocess_for_yolo.py with absolute paths.
# Only needed for YOLOv8 workflow (Workflow A)

train: ./yolo_dataset/train/images
val: ./yolo_dataset/val/images
test: ./yolo_dataset/test/images

# number of classes
nc: 4

# class names
names: ['AB', 'HW', 'KW', 'UndBio']

