# @package _global_
data:
  test_dataset:
    _target_: data.InferenceDataset
    file_path: '../../finding_willy/recordings_2018_filtered_new/180914_150127.wav'
    preprocessors: ${_preprocessors}
    seq_length: 1
    data_sample_rate: ${data.data_sample_rate}
    sample_rate: ${data.sample_rate}
  batch_size: 64
  num_workers: 2
  sample_rate: 16000
  data_sample_rate: 44100