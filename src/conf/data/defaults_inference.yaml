# @package _global_
data:
  batch_size: 64
  num_workers: 10
  sample_rate: 16000
  data_sample_rate: 44100
  test_dataset:
    _target_: data.ClassifierDataset
    data_path: '../../finding_willy/data/'
    mode: val
    metadata_path: '../combined_annotations_filtered_test.csv '
    augmentations_p: 0
    augmentations: null
    preprocessors: ${_preprocessors}
    seq_length: 1
    len_buffer: 0.1
    data_sample_rate: ${data.data_sample_rate}
    sample_rate: ${data.sample_rate}
    equalize_data: false
    slice_flag: true
