# @package _global_
defaults:
  - ../data: barkley_orca
  - ../augmentations: _augmentations
  - ../preprocessors: _preprocessors
  - ../model: defaults
  - ../optim: defaults
  - ../experiment: defaults
