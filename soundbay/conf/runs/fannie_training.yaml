# @package _global_
defaults:
  - ../data: fannie_blue
  - ../augmentations: _augmentations
  - ../preprocessors: _preprocessors
  - ../model: defaults # check in future after first run
  - ../optim: defaults
  - ../experiment: defaults