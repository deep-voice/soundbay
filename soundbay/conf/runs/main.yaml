# @package _global_
defaults:
  - ../data: defaults
  - ../augmentations: _augmentations
  - ../preprocessors: _mel_preprocessors
  - ../model: defaults
  - ../optim: defaults
  - ../experiment: defaults