# @package _global_
defaults:
  - data: defaults
  - augmentations: _augmentations
  - preprocessors: _preprocessors_unit
  - model: defaults
  - optim: defaults
  - experiment: defaults
  - checkpoint: defaults