# @package _global_
data:
  test_dataset:
    _target_: soundbay.data.InferenceDataset
    file_path: '../data/file1.wav'
    data_sample_rate: ${data.data_sample_rate}
    sample_rate: ${data.sample_rate}
  batch_size: 64
  num_workers: 2
  data_sample_rate: 44100
save_raven: True
threshold: 0.5
